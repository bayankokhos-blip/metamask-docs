### Allowlist your bundle ID

To allowlist your app:

1. In the [Embedded Wallets dashboard](https://dashboard.web3auth.io), select your project.

1. Select the **Domains** tab. In **Allowlist URLs**, add `<bundleId>://auth`.
